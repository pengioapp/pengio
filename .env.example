# Copy to .env and fill in from Supabase: Project Settings -> API keys.
# The publishable/anon key is safe in the frontend; it is subject to row level
# security. Never put the service_role key here -- it bypasses every policy.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""
